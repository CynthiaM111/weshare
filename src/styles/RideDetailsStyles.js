import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: '#f9f9f9',
        paddingHorizontal: 10,
        borderRadius: 5,
        marginTop: 5, // Ensures downward expansion
    },
    detailsWrapper: {
        paddingVertical: 10,
    },
    detail: {
        fontSize: 14,
        color: '#555',
        marginBottom: 5,
    },
});
